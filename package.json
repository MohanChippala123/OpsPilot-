{
  "name": "ops-assistant-mvp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "install:all": "npm install --workspaces",
    "build": "npm run build --workspace frontend && node scripts/sync-vercel-dist.js",
    "check": "npm run build",
    "start": "npm run start --workspace backend",
    "seed": "npm run seed --workspace backend"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
